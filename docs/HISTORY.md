# IGVC Unity Simulation

Latest: [white ramp-edge lanes and observed-surface perception](docs/RAMP_EDGE_PERCEPTION.md) completed the seeded course **82/82 checkpoints**, including the ramp and return to start. The audit passed with no resets, gaps or line-guard interventions; sampled clearance was 0.70328 m and maximum observed speed 2.2 m/s. This supersedes the earlier ramp-perception failures described below. Edge projection remains an approximate sensor model.

The [ramp is integrated into procedural course variants](docs/COURSE_RAMP.md), with caster suspension, clear approaches and six ordered ramp checkpoints. The rebuilt course passed generator, Unity and live manual traversal checks. Autonomous ramp perception remains pending; the earlier 78-checkpoint full-loop success belongs to the archived flat course.

The latest [moving terrain perception experiment](docs/TERRAIN_PERCEPTION.md) passed all 10 motion checks but exposed ground misclassification and false lane detections on an unpainted bench. Managed perception-only sessions and exact-frame depth capture/replay make these failures reproducible. Autonomous ramp perception remains pending.

Rear casters now [swivel with local motion](docs/CASTER_SWIVEL.md), including reversing and pivots, with continuous joint feedback and matching Unity/RViz rotation. The response is bounded and configurable; stopped casters hold their angle. This is a motion-aligned approximation, with suspension contact sampling still separate.

The suspension bench passed [turning validation](docs/CASTER_TURNING.md): a full ramp pivot with zero measured axle drift, three measured arcs, and 48 additional Unity support checks. `tools/igvc.ps1 terrain-turn-test` repeats the bounded manual experiment from a fresh bench origin. This extends test coverage; it does not add autonomous ramp navigation or calibrated contact physics.

The terrain bench now has [experimental rear caster suspension](docs/CASTER_SUSPENSION.md): two bounded rear sliders and a spring/damper pitch response, with matching Unity and RViz movement. A one-caster bump and ramp passed live checks at 0.6 and 2.2 m/s. Settings are provisional; this is a reduced-order support model, not calibrated contact physics. Normal course motion remains planar with both suspension joints at rest. Use `terrain-start -Visible` for suspension or add `-RigidTerrain` for the earlier rigid bench.

The [experimental terrain-body bench](docs/TERRAIN_BODY.md) now adjusts body height/attitude from provisional ground supports and publishes dynamic body TF. Slow and 2.2 m/s manual ramp checks passed; `terrain-build` / `terrain-start -Visible` select the bench. Terrain, drive and course players were rebuilt. The normal course still uses planar motion; this is not autonomous ramp traversal.

The [ideal depth camera](docs/DEPTH_CAMERA.md) estimates ground from observed depth and filters obstacles relative to that plane. RGB lanes and hazards now use the same fresh, preceding observed plane through [terrain-relative projection](docs/TERRAIN_LANES.md). With steady ROS timers, the previous flat seed-2027 normal run **completed all 78 checkpoints without pauses, recoveries or line-guard blocks**. Its [audit](artifacts/courses/seed-2027/previous-20260909T202724060370Z/validation.json) passed with 0.70579 m minimum sampled clearance, 0.54300 m conservative swept clearance, 0.29383 m finish error and 2.2 m/s maximum observed speed. Earlier validation passed 42 perception tests, three GPU-rendered terrain fixtures and 12 live depth checks; these were not rerun for the body-TF-only regression. [Timing evidence](docs/ROS_TIMING.md) records continuous depth health and successful loss-stop/no-rearm watchdog checks with unchanged thresholds. This models geometric depth, not OAK-D stereo uncertainty or terrain contact.

Seeded [procedural course variants](docs/PROCEDURAL_COURSES.md) add lane-shape changes, barrels, barricades and dark bowl potholes, with a 10° downward camera and separate camera-hazard costmap layer. The refreshed gallery uses the current successful 2027 run; seed 2028/easy (78/78) and 2029/hard (81/81) remain historical passes from before depth integration. Earlier failed attempts and RGB hazard evidence remain archived. Geometry uses ideal planar motion without vertical contact; one locally stationary plane cannot represent nonplanar transitions, so this is not complete ramp navigation.

The [full-course rolling-horizon run](docs/FULL_COURSE.md) **completed all 77 checkpoints**: 153.333311 m traveled, 99.7987% of planned route arc, and 0.293905 m final position error. Its continuous audit passed with no recoveries, backups, line-guard blocks or timestamp gaps. Sampled minimum barrel clearance was 0.212114 m with no intersections; the opening straight held 2.2 m/s. Painted/unmarked transitions still stop by design. See the [trajectory](artifacts/checks/full-course-trajectory.png), [live report](artifacts/checks/full-course-live.json) and [clearance audit](artifacts/checks/full-course-clearance.json). This is ideal kinematic validation, not contact physics. The associated build passed 28 Unity checks and built all seven ROS packages.

The course includes scroll zoom, right-mouse orbit and robot-follow controls (`F`; `H` for overview). [Nav2 navigation](docs/NAVIGATION.md) uses lidar plus [camera-detected lane boundaries](docs/LANE_DETECTION.md). An independent Unity guard prevents the conservative robot footprint crossing the course's painted lines. [Synthetic GPS waypoint missions](docs/GPS_WAYPOINTS.md) use `gps-list` and `gps-run`. Manual commands take priority and latch autonomy off; stale lane detections also stop autonomy. Physical contact and real GNSS localization remain pending.

The [SoonerRobotics AutoNav course is imported](docs/SOONER_COURSE.md) as a separate Unity scene with R3-a, 41 barrels and the original baked lane layout. `tools/igvc.ps1 course-build` and `course-start` select it. The Windows build and seven live sensor-stream checks passed; this is a 2026 reference course with ideal motion, not proof of 2027 compliance.

Status: the actual R3-a robot drives from ROS commands in Unity and displays in RViz, with its front lidar and pole-top tilting camera mounted. Drive wheels are at the front and casters at the rear. The ideal-motion build has passed 27 general live checks and 48 camera checks. Contact physics, stereo uncertainty and hardware-grade perception/localization remain future milestones. Start with [R3-a drive commands and evidence](docs/R3A_DRIVE.md); the separate [static inspection viewer](docs/ROBOT_INSPECTION.md) is also available.

Build a Unity simulation of the R3-a robot that exchanges standard ROS 2 Jazzy messages, supports RViz2 and Nav2, and can be operated and evaluated from terminals.

The supplied [RPLIDAR A1 model is now mounted in the front housing](docs/LIDAR_MOUNT.md), with Unity scan origin and ROS TF aligned. The latest mounted-robot run passed 27 live checks; internal optical calibration remains provisional.

The [OAK-D Pro now sits on the pole-top pitch bracket](docs/CAMERA_MOUNT.md). Terminal tilt control moves the bracket, camera and optical TF together; RGB has synchronized CameraInfo. Its dedicated live camera checks passed. Ideal geometric depth is implemented; hardware optical calibration and stereo uncertainty remain pending.

Start with [the runnable transport probe runbook](docs/RUNBOOK.md) and [Unity project context](docs/AI/UnityProjectContext.md). Review [the implementation plan](docs/PLAN.md), [the target ROS interface contract](docs/ROS_INTERFACE.md), [the future workflow design](docs/WORKFLOW.md), and [the inspected baseline](docs/BASELINE.md) for the broader scope. The runbook describes current commands; the contract and workflow include unimplemented targets.

[SoonerRobotics reference review](docs/SOONER_REFERENCE_REVIEW.md) documents useful course/camera patterns and the differences from our Jazzy, robot and sensor requirements.

[2025/2026 autonomy review](docs/SOONER_AUTONOMY_REVIEW.md) identifies their Jazzy-based 2025 stack, the 2026 simulator client, and useful perception, launch and replay patterns to adapt.

The implemented probe uses Windows Unity 6000.3.23f1 with the Built-in Render Pipeline plus ROS 2 Jazzy on Ubuntu 24.04 in WSL2. The project targets IGVC 2027 AutoNav only, with an OAK-D Pro and RPLIDAR A1; the current ideal body, RGB and raycast scan are transport fixtures, not calibrated hardware models. The user also supplied the 2026 rulebook as a reference; retain the 2027 competition target and track rule versions separately. Sensor mounting, hardware revisions and operating settings remain open.

The final 600-second run passed all 15 live checks: RGB 13.34 Hz, lidar 5.26 Hz, odometry 50 Hz and real-time factor 0.99997. All 8 reconnect checks and 12 Unity behavior checks passed. RViz visibly displays RGB, lidar, TF and odometry. See [validation evidence](docs/TRANSPORT_EVIDENCE.md) and [the runbook](docs/RUNBOOK.md) for commands, reports and limits.

[Agent workflow](docs/AGENT_WORKFLOW.md) describes bounded delegation and file ownership. The [Cursor review runner](tools/agents/README.md) authenticated and completed a Grok 4.6 review using 12,597 reported tokens. Reviews use selected text files and disabled tools. The default is now medium effort; high effort and Fast can be selected for bounded work when useful. The coordinator checks findings before applying changes.
